import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main:'#8e9aaf'
        },
        secondary:{
            main:'#cbc0d3'
        }
    }
})

export default theme;